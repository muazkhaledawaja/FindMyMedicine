import {
  Gender,
  UserRoles,
  HttpStatus as ResponseCodes,
  UserStatus,
} from "./";

export const GenderValues = Object.values(Gender);
export const UserRolesValues = Object.values(UserRoles);
export const ResponseCodesValues = Object.values(ResponseCodes);
export const UserStatusValues = Object.values(UserStatus);

